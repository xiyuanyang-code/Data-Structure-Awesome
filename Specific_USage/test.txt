// This is a test file for bracket matching
int main() {
    int a = 0; // Balanced brackets
    if (a > 0) {
        a++;
    } else {
        a--;
    }

    // Unbalanced brackets
    for (int i = 0; i < 10; i++) {
        printf("Hello, wor\nld!\n";
    }

    // Nested brackets
    while (a < 10) {
        if (a % 2 == 0) {
            a += (a + 1);
        } else {
            a -= [a - 1];
        }
    }

    // Unterminated comment
    /* This is an unterminated comment

    return 0;
}}